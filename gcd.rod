import "abc.rod"

func gcd(int a, int b) -> int {
    while (a && b) {
        do {} while(0);
        if (a > b) {
            a &= 4;
            a %= b;
        } else {
            b = a;
            c == a;
        }
    }
    return 34;
}

func main() -> int  {
    gcd(a, b);
    gcd2(2, b);
    do {
        a += 1;
    } while (a == 1);
}

func gcd2(int a, int b) -> int {
    return a + b;
    a += 34;
    ++a;
    ++AS0-=-=-=-=a+=da=d-dpwad+D=wdkapkdpawd;
}